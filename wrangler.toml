name = "telegram-private-chatbot"
main = "src/index.js"
compatibility_date = "2024-11-30"

# --- KV 命名空间绑定 ---
# 取消下面注释并填写你自己的 KV Namespace ID 后即可部署。
# 也可以在 Cloudflare Dashboard → Settings → KV Namespace Bindings 手动绑定。
#[[kv_namespaces]]
#binding = "TOPIC_MAP"
#id = "YOUR_KV_NAMESPACE_ID"

# --- 环境变量 ---
# 值留空：实际运行时从 Cloudflare Dashboard → Settings → Variables 读取。
# 在这里声明是为了防止部署后变量丢失。
[vars]
BOT_TOKEN = ""
SUPERGROUP_ID = ""
ADMIN_IDS = ""
